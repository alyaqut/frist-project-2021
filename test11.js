/*textBox = document.querySelector("textarea"),
btn     = document.querySelector(".btn-post");
function textTospeech(text) {
    let uttu = new SpeechSynthesisUtterance(text);
    speechSynthesis.speak(uttu);
}
btn.addEventListener("click" , ()=> {
    if(textBox.value != "") {
        textTospeech(textBox.value);
    }
})*/